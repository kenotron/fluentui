import * as React from 'react';
import { ICheckboxProps, ICheckboxState } from './Checkbox.types';
export declare const useCheckbox: (props: ICheckboxProps, forwardedRef: React.Ref<HTMLElement>) => ICheckboxState;
