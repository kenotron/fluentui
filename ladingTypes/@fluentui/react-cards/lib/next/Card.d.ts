import * as React from 'react';
import { CardProps } from './Card.types';
/**
 * Define a styled Card, using the `useCard` hook.
 */
export declare const Card: React.ForwardRefExoticComponent<Pick<CardProps, string | number> & React.RefAttributes<HTMLElement>>;
