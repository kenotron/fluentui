import { CardState } from './Card.types';
/**
 * The useCard hook processes the Card draft state.
 * @param draftState - Card draft state to mutate.
 */
export declare const useCardState: (draftState: CardState) => void;
